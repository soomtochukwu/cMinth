"use client";
import "./globals.css";

const page = () => {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <div className=" flex flex-col items-center">
        <h1 className="text-3xl font-bold ">Welcome to Cr8or 🎭!</h1>
        <span className="text-sm">
          ...everything is still in template format
        </span>
      </div>
    </div>
  );
};

export default page;
